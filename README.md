# Inverted_search
Designed and implemented an Inverted Search Engine in C that indexes words from multiple text files and enables fast keyword-based search. The system creates an inverted index using hash tables and linked lists to map words to files and their frequency of occurrence.
